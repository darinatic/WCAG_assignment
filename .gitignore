# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# --- Secrets ---
.env

# --- Node ---
scanner/node_modules/

# --- Runtime state (regenerated by running the app) ---
data/
*.log

# --- Corpus cache -------------------------------------------------------------
# chunks.jsonl and embeddings.npy ARE committed so retrieval works offline.
# These are the 22MB of raw fetched W3C HTML they were built from; re-fetch with
# `python scripts/build_corpus.py` if you need to rebuild.
corpus_data/raw/

# --- Superseded by MLflow -----------------------------------------------------
# Eval results live in mlruns/, which IS committed. This directory is the older
# JSON-file output and is no longer written to.
evals/results/

# --- Scratch ---
*.bak
fixtures/_case-*.html

# --- Assignment brief (not mine to publish) ---
Take-home Assignment.pdf

# --- Agent / tooling artifacts ---
.claude/
CLAUDE.md
AGENTS.md
.mcp.json
.playwright-mcp/
.deepeval
.deepeval_telemetry.txt
# Author's own code-level notes and agent-generated specs. DESIGN.md is the
# deliverable; docs/CODEBASE.md declares itself private working notes.
docs/

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
