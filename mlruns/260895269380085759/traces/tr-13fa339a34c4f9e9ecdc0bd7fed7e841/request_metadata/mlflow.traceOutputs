{"answer": "**What's wrong**\nTwo things stand out even though the measured LCP (76ms) is fine: the LCP image is a 1,043,802-byte PNG loaded with no priority hint, and `print.css` is marked render-blocking with no `media` attribute — so it delays ...